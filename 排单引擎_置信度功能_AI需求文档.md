# 排单引擎 · 置信度功能 · AI开发需求

> 给另一个AI改代码用。只写做什么、规则是什么、变量在哪，不写怎么实现。

---

## 一、要做什么

排单引擎跑完每个SKU后，在写入`销售订单明细表`时，新增写入2个字段：

| 字段名 | 飞书类型 | 每次排单都写 |
|--------|---------|------------|
| 已分配数量 | 数字 | 是 |
| 置信度 | 单选（高/中/低） | 是 |

这两个字段已在飞书Base的`销售订单明细表`中手动创建好了。Python侧只需像写`库存可用量`、`缺口数量`一样写这两个字段的值即可。

---

## 二、涉及的表

**销售订单明细表**

| 属性 | 值 |
|------|-----|
| Table ID | `tblJn5iP6imjzE8h` |
| 环境变量 | `TABLE_ID_ITEMS` |

**新增字段（Python写入）：**

| 字段名 | 类型 | 写入规则 |
|--------|------|---------|
| 已分配数量 | 数字 | 每次排单写入，重跑覆盖 |
| 置信度 | 单选 | 每次排单写入，重跑覆盖。选项：高/中/低 |

**已有的相关字段（Python排单循环中已有的变量）：**

| 字段 | 说明 | 排单循环中对应的变量 |
|------|------|-------------------|
| 合同数量 | 订单要求多少件 | `demand` |
| 库存可用量 | 当前排到这个SKU时还剩多少库存 | `remaining`（扣减后的余量） |
| 缺口数量 | 需求-已分配 | `shortage` 或 `demand - allocated` |
| 库存状态 | 有货/缺货 | 库存可用量>0 → 有货，否则缺货 |
| 预计到货日期 | 供应商预计到货日 | `est_date` |
| SKU编码 | 从SKU标准表查找引用 | 明细表字段，查找引用自SKU标准表 |
| 产品名称 | 明细表现有字段 | `product_name` |
| 规格 | 明细表现有字段 | `spec` |

---

## 三、已分配数量的计算逻辑

```python
# 排到这个SKU时
allocated = min(demand, remaining)   # 能分多少分多少，不能超过库存余量
remaining = remaining - allocated    # 扣减库存（已有逻辑，保持不变）

# 写入
row["已分配数量"] = allocated
```

---

## 四、置信度的判定规则

```python
# 输入变量（排单循环里已有）：
#   remaining  — 排到这个SKU时，实时库存扣减后的剩余量（扣减前的值，不是扣减后的）
#                注意：remaining 是用 min(demand, remaining) 之前的那个值
#   spec       — 该SKU的规格（明细表字段"规格"）
#   product_name — 该SKU的产品名称（明细表字段"产品名称"）
#   sku_code   — 该SKU的SKU编码（明细表字段"SKU编码"，可能为空）


# ============================================================
# 置信度判定
# ============================================================

if remaining > 5:
    confidence = "高"

elif 1 <= remaining <= 5:
    confidence = "中"

else:  # remaining <= 0，即库存耗尽了
    if spec == "定制化硬件":
        confidence = "低"

    elif spec == "AC-RB800":
        if "电话" in product_name:
            confidence = "低"
        elif sku_code is None or sku_code == "":
            confidence = "低"
        else:
            confidence = "中"

    else:
        # 外采设备、辅材、转接板、软件等所有其他规格
        confidence = "中"

# 写入
row["置信度"] = confidence
```

### 规则说明（供理解，不需要翻译成代码）

- 库存扣减后还有**6件以上** → 放心，也不用进货
- 库存只剩**1到5件** → 下一个大单来就不够了
- 库存**刚好用完或已经不够** → 看产品类型
  - **定制化硬件**：生产周期长、意外多 → 肯定下个要等
  - **AC-RB800带"电话"**：特殊批次，生产排期没准数 → 肯定下个要等
  - **AC-RB800且SKU编码为空**：数据不完整，没编码说明新产品没走标准流程 → 肯定下个要等
  - **AC-RB800其他情况**：标准批次，按历史节奏能估算 → 一般
  - **其他所有规格**（外采设备、辅材、转接板、软件等）：外采比定制容易补 → 一般

---

## 五、写入时机

在现有的排单循环中，每个SKU排完之后、写`库存可用量`/`缺口数量`/`库存状态`/`预计到货日期`的同一位置，写入`已分配数量`和`置信度`。

伪代码位置：

```python
for order in orders:
    for item in order.items:
        # ... 库存匹配逻辑（已有）...
        
        # 已有：写库存相关字段
        item["库存可用量"] = remaining_after
        item["缺口数量"]   = shortage
        item["库存状态"]   = "有货" if shortage == 0 else "缺货"
        item["预计到货日期"] = est_date
        item["排单批次号"]   = batch_id
        
        # 🆕 新增：写分配数量和置信度
        item["已分配数量"] = allocated
        item["置信度"]     = confidence

# 批量写回明细表（已有逻辑，字段列表加上两个新字段即可）
```

---

## 六、不需要做的事

- 不需要新建表。两个字段加在现有的`销售订单明细表`上
- 不需要改飞书Base结构。字段已手工建好
- 不需要改OA导入逻辑。OA导入不影响这两个字段（排单时覆盖写入）
- 不需要通知/推送。纯数据写入
- 不需要"AI决策理由"字段。已确认不需要
- 不需要"决策类型"字段。已确认不需要
